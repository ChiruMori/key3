$().ready(function(){let aTags=$(".token-tag");let tokenSuffix="?"+CONST_VAL.tokenQueryName+"="+utils.readCache(CONST_VAL.tokenKey);for(let i=0,len=aTags.length;i<len;i++){aTags[i].href=aTags[i].href+tokenSuffix}});